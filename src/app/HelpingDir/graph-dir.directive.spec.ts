import { GraphDirDirective } from './graph-dir.directive';

describe('GraphDirDirective', () => {
  it('should create an instance', () => {
    const directive = new GraphDirDirective();
    expect(directive).toBeTruthy();
  });
});
